package ListofNotes;
import Notes.Notes;
public class Two extends Notes {
    public Two(int denomination, double amount, int quantity) {
        super(denomination, amount, quantity);
    }
}
